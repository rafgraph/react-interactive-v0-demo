import style from './style';

const section = Object.create(style);

section.root = {
  marginBottom: '15px',
};

section.title = {
  fontSize: '18px',
};

section.subTitle = {
  fontSize: '15px',
  color: 'rgb(128, 128, 128)',
};

export default section;
